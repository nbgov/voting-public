
export interface TelegramDecryptResult {
  token: string
}

export interface TelegramDecryptData {
  token: string
}

export interface TelegramAuthPollData {
  token: string
  poll: string
}

export interface TelegramAuthPollResult {
  token: string
}
