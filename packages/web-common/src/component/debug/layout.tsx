import Container from '@mui/material/Container'
import type { FunctionComponent, PropsWithChildren } from 'react'

export const DebugLauout: FunctionComponent<PropsWithChildren> =
  ({ children }) => <Container>{children}</Container>
